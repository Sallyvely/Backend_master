function solution(n, k) {
    const answer = [];
    let temp= n.toString(k);
    let sosu = (n/2~~n)!=0;
    
    if(temp.charAt(i)/i+2!=0)
        answer.push(temp.charAt(i));
    return answer.length;
}